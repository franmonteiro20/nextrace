plugins {
    id 'com.android.application'
}

android {
    namespace 'com.nextrace.app'
    compileSdk 35

    defaultConfig {
        applicationId "com.nextrace.app"
        minSdk 24
        targetSdk 35
        versionCode 5
        versionName "1.2.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
}
